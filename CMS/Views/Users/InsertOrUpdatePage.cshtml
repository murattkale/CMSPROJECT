
@{
    Layout = null;
    string pageTitle = "User";
    string User_ControlNameUrl = "User";
    string FormName = "frm_" + User_ControlNameUrl + "_";
    int User_idUrl = Context.Request.Query["id"].ToInt();
    var User_idUrl_select = Context.Request.Query["selectid"].ToStr();
    string title = User_idUrl > 0 ? (pageTitle + " Düzenle") : (pageTitle + " Kaydet");

    var edit = ViewBag.edit as User;
    if (edit == null)
    {
        edit = new User();
    }
    else
    {
        //User_idUrl_select = edit.KurumId.ToStr();
    }

}

<script type="text/javascript">
    var User_FormName = "@Html.Raw(FormName)";
    var User_ControlNameUrl = "@User_ControlNameUrl";
    var User_idUrl = "@User_idUrl";
    var User_idUrl_select = "@User_idUrl_select";
    var User_edit = @Html.Raw(Json.Serialize(edit));
</script>



<style>
    .modal-dialog {
        width: 75% !important;
    }
</style>

<!-- begin:: Content -->
<div style="width:100%;" class="kt-container  kt-grid__item kt-grid__item--fluid">

    <div class="kt-portlet kt-portlet--mobile">
        <div class="kt-portlet__head kt-portlet__head--lg">

            <div class="kt-portlet__head-label">

                <span class="kt-portlet__head-icon">
                    <i class="kt-font-brand flaticon2-line-chart"></i>
                </span>
                <h3 class="kt-portlet__head-title">
                    @Html.Raw(title)
                </h3>
            </div>
            <div class="kt-portlet__head-toolbar">
                <div class="kt-portlet__head-wrapper">
                    <div class="kt-portlet__head-actions">
                        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                            <span aria-hidden="true">&times;</span>
                        </button>

                    </div>
                </div>
            </div>
        </div>

        <div class="kt-portlet__body">
            <form name="@FormName" id="@FormName">
                <div class="form-horizontal">

                    @Html.Raw(edit.DynamicInputHelper(new string[] { }, new string[] { "TownId", "CityId" }))

                    <partial name="../UserRole/Index" />

                    <div class="form-group">
                        <div class="col-md-offset-9 col-md-9">
                            <input id="btnOK" type="submit" value="@title" class="btn btn-brand btn-elevate btn-icon-sm" />
                        </div>
                    </div>

                </div>
            </form>
        </div>
    </div>
</div>
<!-- end:: Content -->


<script type="text/javascript">
    var User_EditFunc;

    $(function () {
        User_EditFunc = {
            load: function () {

                $('#' + User_FormName).submit(function (e) {
                    e.preventDefault();
                    var postModel = $.fn.toForm("#" + User_FormName);
                    $.LoadingOverlay("show");
                    $.ajx(User_ControlNameUrl + "/InsertOrUpdate", { postModel: postModel }, function (resultData) {
                        if (resultData.ResultType.RType != 3) {
                            User_ListFunc.GetPaging();
                            $("#ajaxSub button[data-dismiss='modal']").click();
                        }
                        else {
                            alerts(resultData.ResultType.MessageList[0]);
                        }
                        $.LoadingOverlay("hide");
                    }, function () { location.reload(); });

                });
            },



        };
        User_EditFunc.load();
    });

    //ayarlar();
</script>



