@using Entity.CMSDB
@{
    Layout = null;
    string pageTitle = "Roles";
    string Roles_ControlNameUrl = "Roles";
    string FormName = "frm_" + Roles_ControlNameUrl + "_";
    int Roles_idUrl = Context.Request.Query["id"].ToInt();
    var Roles_idUrl_select = Context.Request.Query["selectid"].ToStr();
    string title = Roles_idUrl > 0 ? (pageTitle + " Düzenle") : (pageTitle + " Kaydet");

    var edit = ViewBag.edit as Roles;
    if (edit == null)
    {
        edit = new Roles();
    }
    else
    {
        //Roles_idUrl_select = edit.KurumId.ToStr();
    }

}

<script type="text/javascript">
        var Roles_FormName = "@Html.Raw(FormName)";
        var Roles_ControlNameUrl = "@Roles_ControlNameUrl";
    var Roles_idUrl = @Roles_idUrl;

    var edit = @Html.Raw(Json.Serialize(edit));

</script>



<style>
    .modal-dialog {
        width: 75% !important;
    }
</style>



<!-- begin:: Content -->
<div style="width:100%;" class="kt-container  kt-grid__item kt-grid__item--fluid">

    <div class="kt-portlet kt-portlet--mobile">
        <div class="kt-portlet__head kt-portlet__head--lg">

            <div class="kt-portlet__head-label">

                <span class="kt-portlet__head-icon">
                    <i class="kt-font-brand flaticon2-line-chart"></i>
                </span>
                <h3 class="kt-portlet__head-title">
                    @Html.Raw(title)
                </h3>
            </div>
            <div class="kt-portlet__head-toolbar">
                <div class="kt-portlet__head-wrapper">
                    <div class="kt-portlet__head-actions">
                        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                            <span aria-hidden="true">&times;</span>
                        </button>

                    </div>
                </div>
            </div>
        </div>

        <div class="kt-portlet__body">
            <form name="@FormName" id="@FormName">
                <div class="form-horizontal">
                    <input type="hidden" id="Id" name="Id" value="@edit.Id" />
                    <input type="hidden" id="IsDeleted" name="IsDeleted" value="@edit.IsDeleted" />
                    <input type="hidden" id="IsStatus" name="IsStatus" value="@edit.IsStatus" />

                    @Html.Raw(edit.getProp(new string[] { }, new string[] { "" }))

                    <div class="form-group">
                        <div class="col-md-offset-9 col-md-9">
                            <input id="btnOK" type="submit" value="@title" class="btn btn-brand btn-elevate btn-icon-sm" />
                        </div>
                    </div>

                </div>
            </form>
        </div>
    </div>
</div>
<!-- end:: Content -->





<script type="text/javascript">
    var Roles_EditFunc;
    var customComment = "";

    $(function () {
        Roles_EditFunc = {
            //getParents: function () {
            //    $('#dp_ParentId').addOptionAjax("/Roles/GetSelect", null, "value", "text", function () {  }, function () {  }, edit.ParentId, '', 'Seçiniz');
            //},
            load: function () {
                //Roles_EditFunc.getParents();

                $('#' + Roles_FormName).submit(function (e) {
                    e.preventDefault();
                    var postModel = $.fn.toForm("#" + Roles_FormName);

                    $.LoadingOverlay("show");

                    $.ajx(Roles_ControlNameUrl + "/InsertOrUpdate", { postModel: postModel }, function (resultData) {
                        if (resultData.ResultType.RType != 3) {
                            Roles_ListFunc.GetPaging();
                            $("#ajaxSub button[data-dismiss='modal']").click();
                        }
                        else {
                            alerts(resultData.ResultType.MessageList[0]);
                        }
                        $.LoadingOverlay("hide");
                    }, function () { location.reload(); });

                });
            },



        };
        Roles_EditFunc.load();
    });


    //ayarlar();
</script>



