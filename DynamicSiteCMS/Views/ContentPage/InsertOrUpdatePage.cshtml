@{
    var edit = ViewBag.edit as ContentPage;
    if (edit == null)
        edit = new ContentPage();
}
@Html.Raw(edit.DynamicInput("",
                        "", "ContentPageId,ContentPageType,LangId,Name,ContentShort,ContentData", "İçerik", "page", "col-md-4", "col-md-8", "col-md-6",
                       "<div class='form-group row float-md-right'><div class='col-md-offset-9 col-md-9'>" +
                       "<input type='submit' value='" + "Kaydet" + "' " +
                       "class='btn btn-brand btn-elevate btn-icon-sm' /></div></div>"
                       ))


<script>
    var typeid = "@edit.ContentPageType";
</script>

<script>


    $(function () {

        var langInt = setInterval(function () {
            $("#dp_LangId").val(1);
            $("#dp_LangId").change();
            $("#dp_LangId").parent().parent().parent().hide();
            clearInterval(langInt);
        }, 2000);

       

    });
</script>